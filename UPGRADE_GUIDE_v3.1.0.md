# 升级到 v3.1.0 快速指南

## ⚠️ 为什么必须重新安装？

v3.1.0 是一个重要更新，修复了 **Lightroom 插件识别结果不准确** 的问题。

由于内部架构升级，**必须完全重新安装**：

| 需要做什么 | 为什么 |
|-----------|-------|
| ✅ 卸载旧版 SuperBirdID | 配置文件格式变化 |
| ✅ 重装 Lightroom 插件 | 需要配合新 API |
| ✅ 重新设置国家/地区 | 新增国家代码字段 |

---

## 🚀 快速升级步骤（5分钟）

### 第一步：卸载旧版本

```bash
# macOS 终端命令
sudo rm -rf /Applications/慧眼识鸟.app
sudo rm -rf /Applications/SuperBirdID.app

# （可选）清理旧配置
rm -rf ~/Documents/SuperBirdID_File
```

**Lightroom 插件卸载**：
1. Lightroom → 文件 → 增效工具管理器
2. 移除 "SuperBirdID Plugin"
3. 删除插件文件夹

---

### 第二步：安装新版本

1. **下载文件**：
   - `慧眼识鸟-v3.1.0.dmg`（主程序）
   - `SuperBirdIDPlugin-v3.1.0.lrplugin.zip`（插件）

2. **安装主程序**：
   - 打开 DMG → 拖到"应用程序"

3. **安装 Lightroom 插件**：
   - 解压 ZIP
   - Lightroom → 增效工具管理器 → 添加
   - 选择 `.lrplugin` 文件夹

---

### 第三步：首次配置（重要！）

**启动 SuperBirdID GUI**：
1. 打开程序
2. 点击"⚙️ 高级选项"
3. **选择您的常用拍摄国家/地区**（如：澳大利亚）
4. 设置自动保存 ✅

**验证 API 配置**：
启动后检查窗口底部状态：
```
✅ API 已启动 | 默认地区: AU | eBird筛选: 启用
```

---

### 第四步：验证插件工作

**测试 Lightroom 插件**：
1. 选择一张鸟类照片
2. 文件 → 导出 → SuperBirdID 识别
3. **检查结果**：
   - 应该只显示您所在地区的鸟类
   - 结果与 GUI 直接识别一致

**成功标志**：
- ✅ 识别结果有 eBird 匹配标记
- ✅ 不会出现明显不可能出现在当地的鸟类
- ✅ 与 GUI 拖入照片的结果一致

---

## 🎯 新功能速览

### 智能地理筛选

**有 GPS 的照片**：
```
照片 GPS: 墨尔本 (-37.8°, 145.0°)
      ↓
自动获取墨尔本 25km 范围的鸟类列表
      ↓
只显示该列表中的鸟类
```

**无 GPS 的照片**：
```
检查配置文件: country_code = "AU"
      ↓
获取澳大利亚国家鸟类列表
      ↓
只显示该列表中的鸟类
```

---

## ❓ 常见问题

### Q1: 升级后识别结果变少了？
**A**: 这是正常的！新版本过滤掉了不可能在您所在地区出现的鸟类，**结果更准确**。

### Q2: 如何切换到其他国家？
**A**:
1. 打开 SuperBirdID GUI
2. 高级选项 → 选择新国家
3. 自动保存，立即生效

### Q3: Lightroom 插件显示"无法连接 API"？
**A**:
1. 确保 SuperBirdID GUI 已启动
2. 检查 API 状态（窗口底部）
3. 如未启动，点击"启动 API"按钮

### Q4: 需要重新识别旧照片吗？
**A**:
- 如果旧结果满意 → 不需要
- 如果旧结果有错误 → 建议重新识别
- 新版本更准确，特别是无 GPS 的照片

### Q5: 配置文件在哪里？
**A**: `~/Documents/SuperBirdID_File/gui_settings.json`

检查配置：
```bash
cat ~/Documents/SuperBirdID_File/gui_settings.json
```

应该包含：
```json
{
  "country_code": "AU",    // ← 这个字段是新的
  "use_ebird": true
}
```

---

## 🆘 遇到问题？

### 检查清单

- [ ] 已完全卸载旧版本
- [ ] 已安装 v3.1.0 主程序
- [ ] 已重装 Lightroom 插件
- [ ] 已在 GUI 中选择国家/地区
- [ ] API 状态显示"已启动"
- [ ] 配置文件包含 `country_code` 字段

### 仍有问题？

1. **查看完整发布说明**: `RELEASE_NOTES_v3.1.0.md`
2. **提交 Issue**: 附上配置文件和错误截图
3. **回滚到旧版本**: 如果紧急需要，可暂时使用 v3.0.2

---

## 📊 升级效果对比

| 项目 | 旧版本 v3.0.2 | 新版本 v3.1.0 |
|------|--------------|--------------|
| GUI 识别准确度 | ✅ 高 | ✅ 高 |
| Lightroom 插件准确度 | ⚠️ 较低 | ✅ **高** |
| 无 GPS 照片筛选 | ❌ 无法筛选 | ✅ **使用配置** |
| 结果一致性 | ⚠️ 不一致 | ✅ **完全一致** |
| 配置记忆 | ⚠️ 部分记忆 | ✅ **完整记忆** |

---

**升级后的体验提升**：
- 🎯 Lightroom 插件识别结果与 GUI 完全一致
- 🌍 无 GPS 照片也能享受地理筛选
- 💾 设置一次，永久记住
- 📊 更少的误报，更高的准确度

---

**版本**: v3.1.0
**升级时间**: 约 5 分钟
**建议**: 所有用户尽快升级

---

祝您识鸟愉快！🐦
